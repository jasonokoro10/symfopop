{% extends 'base.html.twig' %}

{% block title %}Publicar un nou producte{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-success text-white">
                    <h2 class="h4 mb-0">Publicar Nou Producte</h2>
                </div>
                <div class="card-body">
                    {{ include('product/_form.html.twig') }}
                </div>
                <div class="card-footer bg-light text-center">
                    <a href="{{ path('app_product_index') }}" class="btn btn-link">Cancel·lar i tornar</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
